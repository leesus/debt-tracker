<!DOCTYPE html>
<html lang="en">
<head>
    <title>Debt Tracker - Auth Success</title>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
</head>
<body>
<script type="text/javascript">
    if (window.opener) {
        window.opener.focus();

        var state = '<%= state %>';
        var user = <%- JSON.stringify(user) %>;
        if (window.opener.debttracker &&
            window.opener.debttracker.authState) {

            window.opener.debttracker.authState(state, user);
        }
    }
    window.close();
</script>
</body>
</html>